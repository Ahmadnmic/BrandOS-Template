# secrets — never commit
.env
.env.*
!.env.example

# per-client intake inputs (site captures + CVI documents)
intake/
molslinjen/

# build + deps
node_modules/
build/
dist/
.cache/

# OS noise
.DS_Store
Thumbs.db
